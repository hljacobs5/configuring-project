const chai = require('chai');
const assert = chai.assert;
const Car = require('../lib/mergeSort.js');

//test to make sure that sorted only has one value
//test to make sure that the order of the array is in increasing value