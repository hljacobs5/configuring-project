let unsortedArray = [8, 3, 1, 7, 5, 2];

for (i = 0; i < unsortedArray.length; i++) {
  
  //take the last index value and make it a pivot
  //compare all other index values to it to see if they are larger or smaller
  //set that pivots value in the original array
  //check the length of the two arrays created 
  //if length of new array(s) is one confirm it is sorted and set the position in original array
  //if length is greater than one, take end value of new array and make a new pivot for the other values in those arrays
  //repeat pivot process

}