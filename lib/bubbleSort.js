let unsortedArray = [8, 3, 1, 7, 5, 2];

for (i = 0; i < unsortedArray.length; i++) { //loops through entire array as many times as there are indexes

  //compare every adjacent pair in the array and swap them if the values are out of order
  //repeat process until array in completely sorted
  //exit for loop

}

//return sorted array
