let unsortedArray = [8, 3, 1, 7, 5, 2];

for (i = 0; i < unsortedArray.length; i++) {

  //split array into two arrays - sorted and unsorted. Filtered should only have one index (0) to start.
  //compare the 0 index of both arrays to see which has a higher value 
  //create new array with index 0 equal to the lower value 
  //compare the new value of index 0 to the other index 0 and repeat process

}
